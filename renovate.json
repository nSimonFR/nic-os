{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "separateMajorMinor": true,
  "enabledManagers": [
    "nix",
    "github-actions",
    "custom.regex"
  ],
  "nix": {
    "enabled": true
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "Version pins that no built-in manager reads: AFFiNE's container tag in a Nix let-binding (hosts/rpi5/affine.nix, installed by affine-sync.service), and the hand-pinned from-source packages under pkgs/. Opt-in per package via a `# renovate:` comment directly above the version, so adding a package here is a deliberate act and the ones left out stay out. Each excluded package says why in its own file.",
      "managerFilePatterns": [
        "hosts/rpi5/affine.nix",
        "pkgs/**/*.nix"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)(?: extractVersion=(?<extractVersion>\\S+))?\\s+version = \"(?<currentValue>[^\"]+)\";"
      ],
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "description": "The Renovate version the self-hosted workflow runs (.github/workflows/renovate.yml), so Renovate keeps itself current.",
      "managerFilePatterns": [".github/workflows/renovate.yml"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+RENOVATE_VERSION: (?<currentValue>\\S+)"
      ]
    }
  ],
  "lockFileMaintenance": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "gramps-web-nix is pinned on purpose: a3f86d5 (gramps-web-api 3.17.0) fails pythonRuntimeDepsCheck against our nixpkgs — needs gramps>=6.0.4, pillow<12, sifts>=1.1 plus yclade/authlib/gramps-gedcom7/flask-smorest. See the comment on the input in flake.nix. Renovate proposed it as #438, it was merged, and it broke main. Re-enable once gramps-web-nix carries those deps.",
      "matchManagers": ["nix"],
      "matchPackageNames": ["gramps-web-nix"],
      "enabled": false
    },
    {
      "description": "hermes-agent is held at a tag on purpose: past that tag its nix/lib.nix asks for a nodejs major our nixpkgs release does not carry, and hermes-messaging sits in home.packages, so toplevel forces it and the WHOLE rpi5 config stops evaluating — `nixos-rebuild` cannot start. Renovate proposed exactly that jump once already, as a routine digest bump, which is why the bot is off here rather than left to re-open it: the constraint lives in nixpkgs, not in hermes, so no bump Renovate can compute is safe. See the comment on the input in flake.nix. Re-enable once nixpkgs carries the major upstream wants, or upstream relaxes the requirement; diff nix/lib.nix's argument list first.",
      "matchManagers": ["nix"],
      "matchPackageNames": ["hermes-agent"],
      "enabled": false
    },
    {
      "description": "AFFiNE cuts a -beta tag most days and a canary daily; only follow stable releases. This is the config:recommended default, pinned here because a beta landing on the Pi unattended is the failure this whole path is built to avoid.",
      "matchDepNames": ["ghcr.io/toeverything/affine"],
      "ignoreUnstable": true
    },
    {
      "description": "claude-code follows the `next` dist-tag, not `latest`. The two diverge by days and the gap is load-bearing: Anthropic gates models on the client version, so `latest` (2.1.278 when this was written) answers --model claude-opus-5-5 with a 400 telling you to upgrade, while `next` (2.1.280) serves it and resolves the bare `opus` alias to it. Following `latest` would mean the newest model is unreachable for as long as the tags disagree. The cost is riding a pre-release channel on purpose — read the release notes on the PR.",
      "matchDepNames": ["@anthropic-ai/claude-code"],
      "followTag": "next"
    },
    {
      "description": "pkgs/ bumps: Renovate rewrites `version` via the regex manager, and postUpgradeTasks recomputes the fixed-output hashes beside it (scripts/nix-fix-hashes.py) before committing, so the bump and its hashes land as one commit. Needs the self-hosted runner (.github/workflows/renovate.yml) — the Mend app ignores postUpgradeTasks. The `hashes` check stays as the gate for what the script cannot derive (vendorHash, pnpmDepsHash). The `src` half only holds because every fetcher under pkgs/ passes an explicit version-bearing `name` — see pkgs/README.md, 'Fixed-output names'.",
      "matchFileNames": ["pkgs/**"],
      "postUpgradeTasks": {
        "commands": ["python3 scripts/nix-fix-hashes.py --write {{{packageFile}}}"],
        "fileFilters": ["pkgs/**/*.nix"],
        "executionMode": "update"
      },
      "prBodyNotes": [
        "`hash` / `npmDepsHash` were recomputed in this same commit by `scripts/nix-fix-hashes.py` (postUpgradeTasks, self-hosted Renovate in .github/workflows/renovate.yml).",
        "`vendorHash` (Go) and `pnpmDepsHash` are the exceptions that script cannot derive from a fetch — if this package pins one, the `hashes` check stays red. Fix it with `lib.fakeHash` + a real build, and the `got:` value from the error.",
        "Check the upstream changelog before merging — a major bump may also need module changes. `affine-mcp-server` 1.x → 3.x, for instance, reworks authentication."
      ]
    }
  ]
}
