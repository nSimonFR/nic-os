# herdr client/server config — ~/.config/herdr/config.toml, wired as an
# out-of-store symlink in home/herdr.nix.
#
# WRITABLE ON PURPOSE: herdr edits this file itself (`onboarding`, the theme
# picker, `herdr config` subcommands), so it cannot be a read-only store path.
# The trade is the same one home/dotfiles/claude-settings.json makes — runtime
# edits land in the checkout and show up as a repo diff, which is how they get
# captured rather than lost.

onboarding = false

[ui]
# Written by herdr itself when the sidebar is collapsed — captured here because
# writing it is what replaced the symlink with a real file, which then blocks
# `home-manager switch`.
sidebar_collapsed_mode = "compact"

# Right-aligned status area — herdr's only session-wide, NON-CLICKABLE surface.
# The sidebar was the other candidate and was rejected twice over: every row
# there is a navigation target, and rate limits are per ACCOUNT, so a space row
# would repeat the same number once per space.
#
# `herdr-usage-bar` is the writeShellApplication wrapper from home/herdr.nix; it
# pins the interpreter and runs home/herdr/usage-bar.py. Called by name, not by
# store path, so a rebuild cannot leave a dangling command here.
#
# 60s is the RENDER interval, not the fetch interval — the script caches, and
# api.anthropic.com/api/oauth/usage answers 429 if actually polled this often.
# timeout covers the ~1s `codex app-server` handshake on a cache miss.
tab_bar_right = [
  { type = "command", command = "herdr-usage-bar", interval_seconds = 60, timeout_seconds = 20 },
]
tab_bar_right_separator = " · "

# Claude's session name (`/rename`) reaches herdr via the terminal title.
#
# `agent` renders `display_agent`, which home/scripts/herdr-agent-code.sh sets to
# `cc` — a literal is not allowed here, the tokens are a closed set. No `fg` on
# the title: a row token takes hex only, which would stop following [theme].
[ui.sidebar.agents.rows_by_agent]
claude = [
  [
    "state_icon",
    "agent",
    "machine",
    "workspace",
    "tab",
  ],
  [
    { token = "terminal_title_stripped", bold = true, dim = false },
  ],
]

[ui.toast]
delivery = "terminal"

# Open Claude somewhere new, in the focused pane's directory. The key picks the
# PLACE; the pane itself then asks whether to fork the calling session, start a
# new one, or resume (the claude-pane-menu in zsh/aliases.zsh). Placement cannot
# be asked for afterwards — a workspace is a different container, not a pane.
#
# `"` and `%` are tmux muscle memory; shift+c mirrors prefix+c (new tab), and
# shift+s is a new space. The stock prefix+v / prefix+minus / prefix+c /
# prefix+shift+n stay as they are, for a plain shell.
#
# `shell`, not `pane`: a `pane` command opens a temporary ZOOMED pane with no
# direction control and closes it when the command exits. Everything here needs
# `herdr pane split` / `tab create` / `workspace create`, so the binding shells
# out to herdr-claude-open (home/herdr.nix).
#
# herdr only splits right or down; there is no "split left".
[[keys.command]]
key = 'prefix+"'
type = "shell"
command = "herdr-claude-open down"
description = "split down + claude"

[[keys.command]]
key = "prefix+%"
type = "shell"
command = "herdr-claude-open right"
description = "split right + claude"

[[keys.command]]
key = "prefix+shift+c"
type = "shell"
command = "herdr-claude-open tab"
description = "new tab + claude"

[[keys.command]]
key = "prefix+shift+s"
type = "shell"
command = "herdr-claude-open workspace"
description = "new workspace + claude"

# zoetrope — draws the focused pane's session as a flow graph with the turn's
# SUBAGENTS nested. `q` closes the popup.
#
# Runs `herdr-zoe` (home/herdr.nix), not upstream's herdr plugin, which is
# broken on a nix install — see home/scripts/herdr-zoe.sh.
[[keys.command]]
key = "prefix+shift+z"
type = "popup"
command = "herdr-zoe"
width = "90%"
height = "90%"
description = "zoetrope: session graph"

[theme]
name = "tokyo-night"
auto_switch = false
